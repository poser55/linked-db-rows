{
  "catalogs": {},
  "aliases": {
    "json-export": {
      "script-ref": "../JsonExport.java",
      "description": "Exports a table and its linked tables as JSON. Writes the JSON to stdout (other output to stderr), you can use > myFile.json to get it in a file."
    },
	"json-import": {
      "script-ref": "../JsonImport.java",
      "description": "Imports a table and its linked tables from JSON."
    }
  }
}
	